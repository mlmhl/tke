import React from "react";
import moment from "moment";
import { Calendar } from "@tea/component/calendar";

export default () => {
  return (
    <Calendar
      disabledDate={date => !date.isAfter(moment())}
      onChange={value => console.log(value.format("L"), value)}
    />
  );
};
