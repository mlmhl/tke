import React, { Fragment } from "react";
import { setLocale } from "@tea/component/i18n";
import { Pagination } from "@tea/component/pagination";
import { Button } from "@tea/component/button";
import { Modal } from "@tea/component/modal";

setLocale("en");

export default function LocaleExample() {
  return (
    <Fragment>
      <Button onClick={() => Modal.confirm({ message: "Are you sure?" })}>
        Delete
      </Button>
      <Pagination recordCount={1000} />
    </Fragment>
  );
}
