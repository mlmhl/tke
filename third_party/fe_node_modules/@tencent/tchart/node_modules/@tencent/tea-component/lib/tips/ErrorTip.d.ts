import React from "react";
import { StyledProps } from "../_type";
export interface ErrorTipProps extends StyledProps {
    /**
     * 错误文案
     * @default "加载失败"
     */
    errorText?: React.ReactNode;
    /**
     * 重试文案
     * @default "重试"
     */
    retryText?: React.ReactNode;
    /**
     * 重试时回调，如果传空，则不进行重试
     */
    onRetry?: () => void;
}
export declare function ErrorTip({ errorText, onRetry, retryText, className, style, }: ErrorTipProps): JSX.Element;
