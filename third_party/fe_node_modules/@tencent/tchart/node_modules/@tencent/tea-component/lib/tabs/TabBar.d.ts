import React from "react";
import { Tab } from "./TabProps";
export interface TabBarProps {
    activeId: string;
    tabs: Tab[];
    onActive: (tab: Tab, evt: React.SyntheticEvent) => void;
    addon: React.ReactNode;
    vertical: boolean;
    maxHeight: number;
}
export declare function TabBar({ activeId, tabs, onActive, addon, vertical, maxHeight, }: TabBarProps): JSX.Element;
