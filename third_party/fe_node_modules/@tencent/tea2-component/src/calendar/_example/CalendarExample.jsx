import React from "react";
import { Calendar } from "@tencent/tea-component/lib/calendar";

export default () => {
  return (
    <Calendar
      onChange={value => console.log(value.format("YYYY/MM/DD"), value)}
    />
  );
};
