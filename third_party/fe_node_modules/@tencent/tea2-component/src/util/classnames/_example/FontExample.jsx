import React from "react";
import { useClassNames } from "@tencent/tea-component/lib/util";
import { Justify } from "@tencent/tea-component/lib/justify";

export default function Demo() {
  const { FontSizeReset } = useClassNames();
  return <Justify right={<a className={FontSizeReset}>测试</a>} />;
}
