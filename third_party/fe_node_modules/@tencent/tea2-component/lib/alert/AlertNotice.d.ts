import React from "react";
import { StyledProps } from "../_type";
export interface AlertNoticeProps extends StyledProps {
    /**
     * 通告标题
     */
    title?: React.ReactNode;
    /**
     * 通告内容
     */
    children?: React.ReactNode;
    /**
     * 是否默认展开
     */
    defaultOpen?: boolean;
    /**
     * 是否展开（受控）
     */
    open?: boolean;
    /**
     * 展开变化回调（受控）
     */
    onOpenChange?: (open: boolean) => void;
}
export declare function AlertNotice({ title, children, className, style, open, defaultOpen, onOpenChange, }: AlertNoticeProps): JSX.Element;
