import React from "react";
import { StyledProps } from "../_type";
export interface CollapsePanelProps extends StyledProps {
    /**
     * 面板唯一标识
     */
    id: string;
    /**
     * 头部标题
     * @docType React.ReactNode | ((active: boolean) => React.ReactNode)
     */
    title?: React.ReactNode | ((active: boolean) => React.ReactNode);
    /**
     * 内容
     */
    children?: React.ReactNode;
    /**
     * **\[Deprecated\]** 请使用 `children` 代替
     * @deprecated
     */
    content?: React.ReactNode;
    /**
     * 展开方向
     * @default "bottom"
     * @version 2.1.0
     */
    position?: "top" | "bottom";
}
export declare function CollapsePanel({ id, title, children, content, position, className, style, }: CollapsePanelProps): JSX.Element;
