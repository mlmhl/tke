import { TableAddon } from "../TableProps";
import { Omit } from "../../_type";
import { PaginationProps } from "../../pagination/Pagination";
/**
 * `pageable` 插件用于支持表格分页
 */
export interface PageableOptions<Record = any> extends Omit<PaginationProps, "recordCount" | "className" | "style"> {
    /**
     * 数据总个数，用于计算页数
     * @default records.length
     */
    recordCount?: number;
}
export declare function pageable(options?: PageableOptions): TableAddon;
