import { TableAddon, TableColumn } from "../TableProps";
/**
 * `mergeable` 插件用于合并单元格。
 */
export interface MergeableOptions<Record = any> {
    headColSpan?: (columnIndex: number, context: {
        column: TableColumn<Record>;
    }) => number;
    colSpan?: (columnIndex: number, recordIndex: number, context: {
        column: TableColumn<Record>;
        record: Record;
        rowKey: string;
    }) => number;
    rowSpan?: (columnIndex: number, recordIndex: number, context: {
        column: TableColumn<Record>;
        record: Record;
        rowKey: string;
    }) => number;
}
export declare function mergeable({ headColSpan, colSpan: bodyColSpan, rowSpan: bodyRowSpan, }: MergeableOptions): TableAddon;
