import React from "react";
import { WithTranslationProps } from "../../i18n";
import { WithConfigProps } from "../../_util/config-context";
import { Combine } from "../../_type";
export interface SingleValueSelectProps extends Combine<WithTranslationProps, WithConfigProps> {
    values: any[];
    inputValue: string;
    onChange?: (value: any[]) => void;
    onSelect?: (value: any[]) => void;
    offset: number;
    maxHeight: number;
}
export interface SingleValueSelectState {
    select: number;
}
export declare class SingleValueSelect extends React.Component<SingleValueSelectProps, SingleValueSelectState> {
    constructor(props: any);
    componentDidMount(): void;
    componentWillReceiveProps(nextProps: SingleValueSelectProps): void;
    handleKeyDown: (keyCode: number) => boolean;
    getValue(select: number): any[];
    move: (step: number) => void;
    handleClick: (e: React.MouseEvent<Element, MouseEvent>, index: number) => void;
    render(): JSX.Element;
}
