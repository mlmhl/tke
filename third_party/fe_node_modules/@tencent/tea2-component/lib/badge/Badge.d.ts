import React from "react";
import { StyledProps } from "../_type";
export interface BadgeProps extends StyledProps {
    /**
     * 颜色主题
     *
     * @default "default"
     */
    theme?: "default" | "success" | "warning" | "danger";
    /**
     * 是否为深色背景
     * @default false
     */
    dark?: boolean;
    /**
     * 是否展示为点的样式
     * @default false
     */
    dot?: boolean;
    /**
     * 是否展示为环的样式
     * @default false
     */
    ring?: boolean;
    /**
     * 徽章中内容
     */
    children?: React.ReactNode;
}
export declare function Badge({ theme, dot, ring, dark, children, className, style, }: BadgeProps): JSX.Element;
