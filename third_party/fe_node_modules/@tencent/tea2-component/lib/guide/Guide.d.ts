import React from "react";
import { BubbleProps } from "../bubble";
export interface GuideStep {
    /**
     * 需要高亮的元素
     */
    element: Element | (() => Element);
    /**
     * 气泡位置
     */
    placement?: BubbleProps["placement"];
    /**
     * 标题
     */
    title?: React.ReactNode;
    /**
     * 描述
     */
    description?: React.ReactNode;
    /**
     * 配图 URL
     */
    image?: string;
}
export interface GuideProps {
    /**
     * 引导步骤
     */
    steps: GuideStep[];
    /**
     * 包含 `children` 时，在 `children` 渲染完成后展示引导
     */
    children?: JSX.Element;
    /**
     * 当前步骤
     */
    current?: number;
    /**
     * 步骤变化回调
     */
    onCurrentChange?: (current: number) => void;
    /**
     * 受控展示
     * @default true
     */
    visible?: boolean;
    /**
     * 隐藏取消按钮
     * @default false
     */
    hideCancelButton?: boolean;
    /**
     * 隐藏上一步按钮
     * @default false
     */
    showBackButton?: boolean;
    /**
     * 取消按钮文字
     * @default "跳过"
     */
    cancelText?: string;
    /**
     * 上一步按钮文字
     * @default "上一步"
     */
    backText?: string;
    /**
     * 下一步按钮文字
     * @default "下一步"
     */
    nextText?: string;
    /**
     * 完成按钮文字
     * @default "完成"
     */
    finishText?: string;
    /**
     * 是否自动滚动元素至视野范围
     * @default false
     */
    autoScrollIntoView?: boolean;
}
export declare function Guide({ steps, children, visible, autoScrollIntoView, current, onCurrentChange, showBackButton, hideCancelButton, cancelText, backText, nextText, finishText, }: GuideProps): JSX.Element;
