import React from "react";
export interface ImageProps {
    /**
     * 文件名
     */
    filename?: React.ReactNode;
    /**
     * 预览图
     */
    image?: string;
    /**
     * 上传进度
     */
    percent?: number;
    /**
     * 文件描述
     */
    description?: React.ReactNode;
    /**
     * 按钮区域内容
     */
    button?: React.ReactNode;
}
export declare function Image({ filename, image, percent, description, button, }: ImageProps): JSX.Element;
