import React from "react";
export interface DraggerProps {
    /**
     * 文件名
     */
    filename?: React.ReactNode;
    /**
     * 预览图，仅在 `filename` 不为空时展示
     */
    image?: string;
    /**
     * 上传进度，仅在 `filename` 不为空时展示
     */
    percent?: number;
    /**
     * 文件描述，仅在 `filename` 不为空时展示
     */
    description?: React.ReactNode;
    /**
     * 按钮区域内容，仅在 `filename` 不为空时展示
     */
    button?: React.ReactNode;
    /**
     * `filename` 为空时展示内容，此时支持拖拽上传
     */
    children?: React.ReactNode;
}
export declare function Dragger({ filename, image, percent, button, description, children, }: DraggerProps): JSX.Element;
