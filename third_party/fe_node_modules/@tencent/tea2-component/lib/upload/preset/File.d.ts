import React from "react";
export interface FileProps {
    /**
     * 文件名
     */
    filename?: string;
    /**
     * 上传进度
     */
    percent?: number;
    /**
     * 按钮区域内容
     */
    button?: React.ReactNode;
}
export declare function File({ filename, percent, button }: FileProps): JSX.Element;
