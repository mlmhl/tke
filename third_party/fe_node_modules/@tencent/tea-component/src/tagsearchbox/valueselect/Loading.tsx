import React from "react";
import { useTranslation } from "../../i18n";
import { DropdownBox } from "../../dropdown";

export const Loading = ({ offset }: { offset?: number }) => {
  const t = useTranslation();
  return (
    <DropdownBox style={{ left: `${offset}px` }}>
      <ul className="tea-option-list" role="menu">
        <li role="presentation" className="is-disabled">
          <div role="menuitem" className="tea-option-list__item">
            {t.loadingText}
          </div>
        </li>
      </ul>
    </DropdownBox>
  );
};
