/* eslint-disable @typescript-eslint/no-unused-vars */
import React from "react";
import { testExamples, dateMock } from "@test/utils";

// 日历的显示跟时间有关，所以要 mock 当前时间
beforeEach(() => dateMock.advanceTo(1569859200000));
afterEach(() => dateMock.clear());

// 测试组件代码 Example 快照
testExamples(__dirname);

test("TODO", () => {
  // add test here
});
