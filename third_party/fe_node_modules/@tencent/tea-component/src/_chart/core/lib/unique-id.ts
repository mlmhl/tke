const uniqueId = (function unique() {
  const map = {};
  return function uniqueInner(pre: string) {
    const prefix = pre || "g";
    if (!map[prefix]) {
      map[prefix] = 1;
    } else {
      map[prefix] += 1;
    }
    return prefix + map[prefix];
  };
})();

export default uniqueId;
