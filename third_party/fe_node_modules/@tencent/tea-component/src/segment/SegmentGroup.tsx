import React from "react";
import { createRocket } from "../_util/create-rocket";

export const SegmentGroup = createRocket("Group", "div.tea-region-pave");
export function SegmentGroupItem({
  name,
  children,
}: {
  name: React.ReactNode;
  children: React.ReactNode;
}) {
  return (
    <div className="tea-region-pave__item">
      {children}
      <span className="tea-region-pave__area-name">
        <i>{name}</i>
      </span>
    </div>
  );
}
