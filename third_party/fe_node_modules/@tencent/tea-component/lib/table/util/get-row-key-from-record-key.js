"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
function getRowKeyfromRecordKey(recordKey) {
    var getRowKey = null;
    if (typeof recordKey === "function") {
        getRowKey = recordKey;
    }
    else if (typeof recordKey === "string") {
        var keyName_1 = recordKey;
        getRowKey = function (record, recordIndex) { return String(record[keyName_1]); };
    }
    else {
        // 如果未提供 rowKey，使用索引生成
        // TODO: 输出告警
        getRowKey = function (record, recordIndex) { return "record_" + recordIndex; };
    }
    return getRowKey;
}
exports.getRowKeyfromRecordKey = getRowKeyfromRecordKey;
//# sourceMappingURL=get-row-key-from-record-key.js.map