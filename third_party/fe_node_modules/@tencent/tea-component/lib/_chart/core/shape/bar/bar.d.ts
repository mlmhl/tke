import { Shape } from "../shape";
import { Context } from "../../context";
import { RectCoordinate } from "../../coordinate/rect";
import { Color } from "../../../type";
export declare class Bar extends Shape {
    /**
     * 绑定的坐标实例
     */
    private coordinate;
    /**
     * 折线对象
     */
    private bar;
    private barWidth;
    constructor(ctx: Context, coordinate: RectCoordinate);
    position(pos: string): this;
    size(size: number): this;
    color(color: string | Color): this;
    axis(axis: Context["axis"]): this;
    draw(highlightIndex?: number): this;
    private showHoverBand;
    private hideHoverBand;
    private onLegendItemHover;
    private onLegendItemLeft;
}
